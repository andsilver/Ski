select#resort_id.form-control name="resort_slug"
  option value=""= t('home.quick_search.choose_resort')
  - if @resorts.empty?
    option value=""= t('home.quick_search.choose_country_first')
  - else
    = options_for_select @resorts.map { |r| [r, r.slug] }
