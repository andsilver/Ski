<script>
$(document).ready(function() {
  $('#gallery_thumbnails a').fancyZoom({scaleImg: true, closeOnClick: true});
});
</script>

<% if @images.empty? %>
  <p>
    There are no photographs of this resort yet.
  </p>
<% else %>
  <div id="gallery_thumbnails" class="thumbnails">
  <% @images.each do |i| %>
    <% t = gallery_thumbnail(@resort, i) %>
    <a href="#pic_<%= Digest::MD5.hexdigest(t) %>"><img src="<%= t %>" alt="Photo of <%= @resort %>"></a>
  <% end %>
  </div>
  <%= clear %>

  <% @images.each do |i| %>
  <div id="pic_<%= Digest::MD5.hexdigest(gallery_thumbnail(@resort, i)) %>"><img src="<%= gallery_photo(@resort, i) %>"></div>
  <% end %>
<% end %>
