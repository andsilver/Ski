<div id="wide_content_with_wide_skyscrapers">

<% if @images.empty? %>
  <p>
    <%= t('resorts_controller.piste_map.no_piste_maps') %>
  </p>
<% else %>
  <% @images.each do |i| %>
    <% t = piste_map(@resort, i) %>
    <p>
      <a href="<%= piste_map_full_size_resort_path(@resort) %>?map=<%= i %>"><img src="<%= t %>" alt="Piste map for <%= @resort %>"></a>
    </p>
  <% end %>
  <%= clear %>
<% end %>

</div>
<div id="wide_skyscrapers">
  <%= render :partial => 'banner_adverts/banners', :object => BannerAdvert.small_banners_for(@resort) %>
</div>
