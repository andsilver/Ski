<% if @current_user.directory_advert_in_basket %>
<p class="warning">
  You are creating a new directory advert.
  It seems you already have a directory advert in your basket.
  Did you mean to edit <%= link_to 'that one', edit_directory_advert_path(@current_user.directory_advert_in_basket) %>?
</p>
<% end %>
<%= render "form" %>
