<% if @current_user.role.advertises_through_windows? %>
<a class="cms_box" href="<%= buy_windows_adverts_path %>">
  <h2><%= t('adverts_controller.buy_advertising_windows') %></h2>
  <p>Windows allow you to swap in and out properties for sale or rent.</p>
</a>
<% end %>
<% if @current_user.role.advertises_generally? %>
<a class="cms_box" href="<%= new_banner_advert_path %>">
  <h2>New Banner Advert</h2>
  <p>Advertise your business throughout a resort's pages.</p>
</a>
<a class="cms_box" href="<%= new_directory_advert_path %>" title="New Directory Advert">
  <h2>New Directory Advert</h2>
  <p>Advertise your business within a resort's directory.</p>
</a>
<% end %>
<% if @current_user.role.advertises_properties_for_rent? %>
<a class="cms_box" href="<%= new_property_path %>" title="New Property for Rent">
  <h2>Advertise a Property for Rent</h2>
  <p>Advertise your property in a resort's property rentals area.</p>
</a>
<% end %>
<% if @current_user.role.advertises_properties_for_sale? %>
<a class="cms_box" href="<%= new_property_path(:for_sale => true) %>">
  <h2>Advertise a Property for Sale</h2>
  <p>Advertise your property in a resort's property sales area.</p>
</a>
<% end %>
