<h1><%= link_to 'Directory', resort_categories_path(@category.resort) %> &gt; <%= @category.name %> in <%= @category.resort.name %></h1>

<table id="directory_adverts">
  <thead>
    <tr>
      <th>Business</th>
    </tr>
  </thead>
  <tbody>
    <% @directory_adverts.each do |da| %>
    <tr>
      <td><%= link_to da.user.business_name, da %></td>
    </tr>
    <% end %>
  </tbody>
</table>
