= bootstrap_form_for([:admin, @region]) do |f|
  = f.text_field :name
  = f.text_field :slug
  = f.label :country_id
  = country_select('region')
  = f.check_box :visible
  = editor(f, :info, :html)
  .form-actions
    = f.submit class: 'btn btn-primary'
