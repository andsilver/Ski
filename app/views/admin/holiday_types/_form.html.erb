<%= form_for([:admin, @holiday_type]) do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>
<%= f.label :slug %>
<%= f.text_field :slug %>
<div class="form-actions">
  <%= f.submit class: 'btn btn-primary' %>
</div>
<% end %>
