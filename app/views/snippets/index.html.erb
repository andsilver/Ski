<%= render 'cms/menu' %>
<div class="cms">
<div class="page-header">
  <h1>Snippets</h1>
</div>

<p>
  Snippets are bits of text and HTML that are displayed around the website. They are identified by their name
  and are set up by your web developer so that you can change the content at any time.
</p>

<p>
  <%= link_to 'New', new_snippet_path, class: 'btn' %>
</p>
<% unless @snippets.empty? -%>
<table id="snippets" class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
<% @snippets.each do |snippet| %>
    <tr>
      <td><%= snippet.name %> (<%= snippet.locale %>)</td>
      <td>
        <%= link_to '<i class="icon-edit"></i> Edit'.html_safe, edit_snippet_path(snippet), class: 'btn' %>
        <%= link_to '<i class="icon-trash icon-white"></i> Delete'.html_safe, { action: "destroy", id: snippet.id },
                   confirm: 'Are you sure?',
                   method: :delete,
                   class: 'btn btn-danger' %></td>
    </tr>
<% end -%>
  </tbody>
</table>
<% end -%>
</div>
