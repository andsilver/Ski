<section>
  <h2><%= @region.name %></h2>
  <ul id="resort-nav">
<% if @region.has_page?('how-to-get-there') -%>
    <li <%= 'class="active"'.html_safe if current_page?(how_to_get_to_region_path(@region)) %>><%= link_to 'How to Get There', how_to_get_to_region_path(@region) %></li>
<% end -%>
  </ul>
</section>
