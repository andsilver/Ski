<% if @property_base_price.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@property_base_price.errors.count, "error") %> prohibited this property base price from being saved:</h2>

    <ul>
    <% @property_base_price.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= form_for @property_base_price do |f| %>
<div class="field">
  <%= f.label :number_of_months %>
  <%= f.number_field :number_of_months %>
</div>
<div class="field">
  <%= f.label :price %>
  <%= f.number_field :price %> EUR
</div>
<div class="field">
  <%= f.submit class: 'btn btn-primary' %>
</div>
<% end %>
