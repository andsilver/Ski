<%= link_to @property.resort.country.name, @property.resort.country %> &gt;
<%= link_to @property.resort.name, @property.resort %> &gt; 
<% if @property.new_development? %>
  <%= link_to t('view.properties._show_property_heading.new_developments'), resort_property_new_developments_path(@property.resort) %>
<% elsif @property.for_sale? %>
  <%= link_to t('for_sale'), resort_property_sale_path(@property.resort) %>
<% elsif @property.for_rent? %>
  <%= link_to t('for_rent'), resort_property_rent_path(@property.resort) %>
<% elsif @property.hotel? %>
  <%= link_to t('hotels'), resort_property_hotels_path(@property.resort) %>
<% end %>
&gt;
<%= @property.name %>
